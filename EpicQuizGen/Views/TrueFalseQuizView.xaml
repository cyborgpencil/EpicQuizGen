<UserControl x:Class="EpicQuizGen.Views.TrueFalseQuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EpicQuizGen.Views"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">
    <StackPanel>
        <RadioButton IsChecked="{Binding Path=TrueAnswer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="True" GroupName="ToF"/>
        <RadioButton IsChecked="{Binding Path=FalseAnswer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="False" GroupName="ToF"/>
    </StackPanel>
</UserControl>
