<UserControl x:Class="EpicQuizGen.Views.QuestionsShowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EpicQuizGen.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="640" d:DesignWidth="1000">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Content="Save" Command="{Binding Path=Parent.DataContext.SaveQuestionCommand, RelativeSource={RelativeSource Self}}"/>
            <Button Content="Update"/>
            <Button Content="New"/>
            <Button Content="Clear"/>
        </StackPanel>
        <TextBlock Text="List of Questrions"></TextBlock>
        <DataGrid ItemsSource="{Binding Questions}" AutoGenerateColumns="True" Height="100" VerticalScrollBarVisibility="Visible"/>
        <ContentControl Content="{Binding Path=QuestionView}"/>
    </StackPanel>
</UserControl>
