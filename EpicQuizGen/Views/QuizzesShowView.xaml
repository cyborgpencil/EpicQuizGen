<UserControl x:Class="EpicQuizGen.Views.QuizzesShowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EpicQuizGen.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock Text="Current Quizzes:"></TextBlock>
        <DataGrid Height="200" ItemsSource="{Binding Path=QuizList, Mode=TwoWay}" IsReadOnly="True"/>
        <StackPanel Orientation="Horizontal">
            <Button Content="New Quiz" Margin="2" Command="{Binding Path=NewQuizCommand}"/>
            <Button Content="Edit Quiz" Margin="2"/>
            <Button Content="Delete Quiz" Margin="2"/>
        </StackPanel>
        <local:QuizView />
    </StackPanel>
</UserControl>
