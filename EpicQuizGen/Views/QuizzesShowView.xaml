<UserControl x:Class="EpicQuizGen.Views.QuizzesShowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EpicQuizGen.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock Text="List of Quizzes"></TextBlock>
        <DataGrid>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Quiz Name" />
                <DataGridTextColumn Header="Creation Date" />
                <DataGridTextColumn Header="Category" />
                <DataGridTextColumn Header="Last Grade" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
